- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.bashrc:
    # ~/.config/kitty/kitty.conf: kitty/kitty.conf
    # ~/.config/kitty/kitty_tokyonight_night.conf: kitty/kitty_tokyonight_night.conf
    # ~/.config/nvim: nvim
    # ~/.dotfiles: ""
    # ~/.gitconfig:
    # ~/.gitignore:
    # ~/.gitmessage:
    # ~/.local/share/marker/user_commands.txt: marker/user_commands.txt
    # ~/.tmux.conf:
    # ~/.tmux/dotfiles.proj:
    # ~/.zshenv:
    # ~/.zshrc:
    # ~/bin: bin

- shell:
    - git submodule update --init --recursive
    # - '[ -n "$TMUX" ] && tmux source-file "${HOME}/.tmux.conf"'
    # - tic -x ./terminfo/tmux-256color.terminfo # custom terminfo w/ italics
